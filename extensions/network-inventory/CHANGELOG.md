# Network Inventory

Hosts and services discovered on the network.

## 0.0.0, unreleased

Prepared, not yet published. The module ships inside Aegis core today; the entry
exists here so it can leave the tree with an update cadence of its own, which is
what `backend/src/config/modules.js` says is coming.
