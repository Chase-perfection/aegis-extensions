{
  "name": "aegis-extensions",
  "version": "0.0.0",
  "private": true,
  "description": "Source, catalogue and release factory for Aegis extensions",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.19.1"
  },
  "scripts": {
    "test": "node --test --test-concurrency=4",
    "build": "node scripts/build-index.mjs",
    "validate": "node scripts/validate-catalog.mjs"
  }
}
